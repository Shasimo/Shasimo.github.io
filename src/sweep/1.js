class Signature{
    constructor(path) {
        this.signature = path; // list of portals and vertices ordered by encounter
    }

    add(elements){
        this.signature.add(elements);
    }

    getLastVertex(){
        return
    }

}

class Envelope{
    // ensemble de fonctions de signatures sachant edge ()
}





class FunSignatureEdge{
    // f _sigma|e
    constructor(signature, interval, distanceLastVertexFromSource, vertex, edge) {
        this.signature;
        this.interval = this.interval; // couple of points
        this.distanceLastVertexFromSource = distanceLastVertexFromSource;
        this.lastVertexPosition;
    }

    compute(p){ // p on edge
        // distance de s à e
    }

}

